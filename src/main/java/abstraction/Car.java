package abstraction;

public interface Car {
    void go();
    void stop();
}


//By default compiler knows that all the methods are abstract.
//Compiler marks each method in interface as public and abstract.